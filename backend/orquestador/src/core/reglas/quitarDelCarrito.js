class QUITAR_DEL_CARRITO {
    ejecutarRegla = ({ servicioColasMensajes,
      configuracionRegla,
      mensajeEntrante,
      manejadorDatosInternos}) => {
       //implementacion
      return null;
    };
  }
  
  module.exports = { QUITAR_DEL_CARRITO };