class INICIAR_CARRITO {
    ejecutarRegla = ({ servicioColasMensajes,
      configuracionRegla,
      mensajeEntrante,
      manejadorDatosInternos}) => {
       //implementacion
      return null;
    };
  }
  
  module.exports = { INICIAR_CARRITO };